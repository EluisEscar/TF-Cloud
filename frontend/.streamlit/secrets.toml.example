# Copia este archivo a frontend/.streamlit/secrets.toml para desarrollo local,
# o configúralo desde Streamlit Community Cloud (Settings → Secrets) al deployar.
# NUNCA commitees el secrets.toml real (está en .gitignore).

API_URL = "http://localhost:8000"
SUPABASE_URL = "https://<PROJECT_REF>.supabase.co"
SUPABASE_ANON_KEY = "eyJhbGciOi..."
