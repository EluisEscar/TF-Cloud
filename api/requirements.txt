fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic>=2.11.7,<3.0.0
# sklearn debe matchear el runtime de Databricks Free Edition (1.6.1), que es
# donde se entrena el modelo semanalmente. Pickles entre minor versions de
# sklearn no son compatibles (el formato del Tree cambia).
scikit-learn==1.6.1
# numpy 2.x: los wheels de sklearn 1.6.1 en PyPI están compilados contra 2.x,
# así que aunque el metadata diga >=1.19, en la práctica necesitamos 2.x para
# evitar el ImportError _multiarray_umath.
numpy>=2.0,<3
pandas==2.2.3
joblib>=1.2.0
huggingface_hub>=0.27.0
boto3>=1.35.0
python-dotenv>=1.0.0
